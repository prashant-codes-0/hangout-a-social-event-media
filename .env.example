# Database
MONGODB_URI=mongodb://localhost:27017/hangouts

# JWT
JWT_SECRET=your-super-secret-jwt-key-here

# Email Configuration (SMTP)
# For development: Leave SMTP_USER empty to use console logging
# For production: Set up real SMTP credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Hangouts App" <noreply@hangouts.com>

# Development Mode (set to 'development' to log emails to console)
NODE_ENV=development

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:4200

# Server
PORT=3000